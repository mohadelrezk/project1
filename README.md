# project1
ML project


### installation

$ pip3 install --upgrade setuptools

$ virtualenv -p python3 venv

$ source  ./venv/bin/activate

$ pip install -r requirements


### running jupyter notebook

$ source  ./venv/bin/activate

$ jupyter notebook

browse to the notebook you would like to explore!